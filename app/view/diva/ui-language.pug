div(class='field')
  div(class='control has-icons-left')
    div(class='select')
      select(name='uiLanguage')
        each language in Culture.getListIdentLanguages()
          option(selected=(language === Culture.uiLanguage)) #{language}
    div(class='icon is-small is-left')
      i(class='fas fa-globe')
